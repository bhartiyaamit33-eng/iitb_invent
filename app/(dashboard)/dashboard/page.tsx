export default function DashboardStubPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <p className="text-sm font-semibold uppercase tracking-[0.14em] text-mute">
        Dashboard · M1 stub
      </p>
      <h1 className="mt-3 font-display text-4xl tracking-wide text-teal-deep">
        Attendee dashboard
      </h1>
      <p className="mt-4 text-ink-soft">
        Auth, registration, and schedule views arrive in later milestones. This
        route group is reserved.
      </p>
    </main>
  );
}
